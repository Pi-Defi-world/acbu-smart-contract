[package]
name = "acbu_escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[dependencies]
soroban-sdk = "=21.7.7"
shared = { path = "../shared" }

[dev-dependencies]
soroban-sdk = { version = "=21.7.7", features = ["testutils"] }
